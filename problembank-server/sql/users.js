module.exports = {

    // SELECT
    selectUserByIdPass: "SELECT * FROM problems.pb_users WHERE user_id = ? AND pw = ?",
    selectAllUsers: "SELECT * FROM problems.pb_users",

    // INSERT

    // UPDATE

    // DELETE
}